# hv — Linux Hyper-V guest integration: the KVP / VSS / fcopy daemons, the lsvmbus
# listing tool, and the KVP helper scripts. Built from tools/hv (build.tools).
#
# The kvp daemon's helper-script directory (#define KVP_SCRIPTS_PATH, default
# /usr/libexec/hypervkvpd/) is rewritten at build time to a triplet path, since
# usr/libexec is not a PSD-009 destination; the helpers install there to match.
# Daemons default to sbin and are redirected to bin (PSD-009 is bin-only).
#
# Note: only useful when Peios runs as a guest ON Microsoft Hyper-V; dead weight
# otherwise — shipped for completeness.
#
# ELF dependencies are derived from the staged daemons. lsvmbus additionally
# needs the packaged Python interpreter. Upstream has no manuals for any of
# them; the pages are Peios' own (build/man/).

# PSPU §5.24: a payload carrying man pages SHOULD declare man-db.
side_effects = ["man-db"]

[package]
name = "dev.peios.hv"
description = "Hyper-V guest integration daemons and tools (from Linux 7.0.9 tools/hv)"

[dependencies]
"org.python.python3" = ">= 3.14, < 3.15"

[provides]
"hv" = "{{version}}-1"

[replaces]
"hv" = "<= 0.20.1-rc12-26"

[files]
"@source:COPYING" = "usr/share/licenses/dev.peios.hv/COPYING"
"tools:usr/bin/hv_kvp_daemon"       = "usr/bin/hv_kvp_daemon"
"tools:usr/bin/hv_vss_daemon"       = "usr/bin/hv_vss_daemon"
"tools:usr/bin/hv_fcopy_uio_daemon" = "usr/bin/hv_fcopy_uio_daemon"
"tools:usr/bin/lsvmbus"             = "usr/bin/lsvmbus"
"tools:usr/lib/x86_64-linux-peios/hypervkvpd/**" = "usr/lib/x86_64-linux-peios/hypervkvpd/"
"tools:usr/share/man/man8/hv_kvp_daemon.8.gz"       = "usr/share/man/man8/hv_kvp_daemon.8.gz"
"tools:usr/share/man/man8/hv_vss_daemon.8.gz"       = "usr/share/man/man8/hv_vss_daemon.8.gz"
"tools:usr/share/man/man8/hv_fcopy_uio_daemon.8.gz" = "usr/share/man/man8/hv_fcopy_uio_daemon.8.gz"
"tools:usr/share/man/man1/lsvmbus.1.gz"             = "usr/share/man/man1/lsvmbus.1.gz"
