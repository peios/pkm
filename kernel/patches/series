security/pkm-lsm-wireup.patch
security/pkm-kacs-builtin-signing-keys-section.patch
security/security-inode-rename-flags.patch
fs/stratafs-wireup.patch
fs/overlayfs-readdir-cache-error.patch
security/commoncap-ptrace-nice-bypass.patch
security/commoncap-capability-switchboard.patch
fs/proc-base-token-files.patch
fs/proc-base-timerslack-cap-bypass.patch
fs/proc-base-mem-open-mode-split.patch
fs/proc-base-io-accounting-query-mode.patch
fs/proc-base-idmap-setgroups-intent.patch
fs/proc-base-metadata-write-gates.patch
fs/proc-base-metadata-read-gates.patch
fs/proc-array-cap-reporting.patch
fs/proc-task_mmu-clear_refs-gate.patch
fs/proc_namespace-mount-metadata-gate.patch
fs/read_write-write-intent.patch
fs/locks-stacked-lease-identity.patch
fs/stat-getattr-gate.patch
fs/statfs-gate.patch
fs/sync-fsync-gate.patch
fs/utimes-gate.patch
fs/xattr-raw-sd-guard.patch
fs/aio-write-intent.patch
fs/fhandle-open-by-handle-gate.patch
fs/file_attr-fileattr-gates.patch
fs/namespace-rootfs-seed-sd.patch
# After namespace-rootfs-seed-sd: that patch edits fs/namespace.c near line
# 6158, this one near 2001. Applying the later-in-file hunk first means
# neither shifts the other, so both land at their recorded offsets.
fs/namespace-mount-volume-privilege.patch
fs/super-mount-volume-privilege.patch
fs/fsopen-reconfigure-volume-privilege.patch
fs/open-faccessat-access-gate.patch
fs/open-fchdir-traverse.patch
fs/open-fchmod-fchown-gate.patch
fs/truncate-metadata-gate.patch
fs/open-fallocate-gate.patch
drivers/devtmpfs-seed-root-sd.patch
io_uring/rw-write-intent.patch
kernel/events-perf-event-open-gate.patch
kernel/pid-pidfd-ptrace-modes.patch
kernel/ptrace-access-check-bypass.patch
kernel/sched-setaffinity-gate.patch
kernel/signal-task-kill-bypass.patch
kernel/sys-prctl-capability-guard.patch
kernel/sys-getuid-getgid-realcred.patch
kernel/umh-mark-usermodehelper.patch
include/cred-projected-fsuid-fsgid.patch
include/ptrace-kacs-modes.patch
mm/mempolicy-movememory-gate.patch
mm/migrate-pages-gate.patch
mm/process_vm_access-rw-mode-split.patch
# PEI-689: the KACS ptrace answer is final through mm_access — no
# perfmon_capable() fallback for READ modes — and /proc/<pid>/fd is gated
# like the entries it lists. Neither file is touched by another patch.
kernel/fork-mm-access-no-perfmon-fallback.patch
fs/proc-fd-dir-ptrace-read-gate.patch
net/sock-peercred-projection.patch
net/socket-sol-kacs-dispatch.patch
net/scm-kacs-token.patch
net/af_unix-kacs-token.patch
# PEI-1115: the privilege half of the port rebind rule. One helper in
# include/net/sock.h and the six owner comparisons in net/ipv4 that call
# it; no other patch touches those files.
net/inet-reuseport-tcb-rebind.patch
ipc/util-lsm-with-object.patch
arch/x86-prctl-shadow-stack.patch
arch/syscall-table-pkm.patch
# PNP (PEI-598): wires net/pnp/ (staged by stage-sources.sh) into the net
# build. Touches only net/{Kconfig,Makefile} one-liners; order-independent.
net/pnp-wireup.patch
# PNP machinery slice: the flow tag store's conntrack extension (enum slot,
# size table, add at flow creation, release at free). Touches conntrack
# files no other patch does; order-independent.
net/pnp-conntrack-ext.patch
# PNP Flow slice: the refusal bit on sk_buff (PNP does not judge its own
# refusals). One hunk in include/linux/skbuff.h; order-independent.
net/pnp-refusal-bit.patch
# PEI-1078: CLONE_INTO_CGROUP captured the parent's cgroup kill_seq, so every
# child cloned into a cgroup that was ever cgroup.kill'ed was SIGKILLed at
# birth. One hunk in kernel/cgroup/cgroup.c; no other patch touches it.
kernel/cgroup-clone-into-cgroup-kill-seq.patch
# PEI-1143: perf's hand-written x86 assembly -- bench's memcpy/memset
# wrappers around the kernel's memcpy_64.S and memset_64.S, and the tests'
# regs_load.S -- got no endbr64 and no IBT/SHSTK property under
# -fcf-protection, so the linked perf lost control-flow protection on a Peios
# root. Each takes <cet.h> and _CET_ENDBR. tools/perf only; no other patch
# touches it.
tools/perf-bench-asm-cet.patch
# PEI-1173: mount namespaces are KACS objects. Creating one alone needs no
# capability (kernel/nsproxy.c); every other namespace type keeps the gate.
# fs/namespace.c mints a creator descriptor in copy_mnt_ns, frees it in
# mnt_ns_release, and asks KACS per operation in may_mount(). The fs patch is
# cut against the tree with namespace-rootfs-seed-sd and
# namespace-mount-volume-privilege already applied and replaces the latter's
# may_mount() body, so it must stay after both.
kernel/nsproxy-mount-namespace-unprivileged.patch
fs/namespace-mount-namespace-objects.patch
# PEI-1173, slice 2: a new tmpfs or proc is admitted by the namespace
# descriptor too. may_mount_op() takes the filesystem type, and the
# superblock gate in fs/super.c asks the same question. Cut against the tree
# with the two patches above applied; stays after them and after
# super-mount-volume-privilege, whose call it replaces.
fs/namespace-mount-namespace-new-fs.patch
fs/super-mount-namespace-objects.patch
